h_mix = h_mix;
road = road;
bldHeight = bldHeight;
verToHor = verToHor;            % for building only?
bldDensity = bldDensity;
treeCoverage = treeCoverage;
sensAnthrop = sensAnthrop;
latAnthrop = latAnthrop;
roadShad = min(treeCoverage/(1-bldDensity),1);
bldWidth = 4*bldHeight*bldDensity/verToHor; 
canWidth = d - bldWidth;
canAspect = bldHeight/canWidth;
roadConf = ((canAspect)^2+1)^(1/2)-canAspect;      
wallConf
facArea = 4*bldWidth*bldHeight;
roadArea = d^2 - (bldWidth)^2;
roofArea = (bldWidth)^2;
canTemp = initialTemp;
roadTemp = initialTemp;
canHum = initialHum;
ublWind = max(initialWind,parameter.windMin);
canWind = initialWind;
ustar = 0.1*initialWind;
ustarMod = 0.1*initialWind;
frontDens = verToHor/4.;
z0u = frontDens*bldHeight;
l_disp = 3*frontDens*bldHeight;
alb_wall = alb_wall;
facAbsor = (1-r_glaze)*(1-alb_wall)+r_glaze*(1-0.75*SHGC);
roadAbsor = (1-road.vegCoverage)*(1-road.albedo);
sensHeat = 0.;